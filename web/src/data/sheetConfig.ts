/**
 * Published-CSV URLs for the live Google Sheet (File → Share → Publish to
 * web → choose the sheet tab → CSV, then paste the resulting link here).
 * See data/README.md for the full setup steps and column reference.
 *
 * Left empty, the site just uses the bundled sample data in listings.ts /
 * testimonials.ts — nothing breaks, there's just no live content yet.
 */
export const LISTINGS_CSV_URL = "";
export const TESTIMONIALS_CSV_URL = "";
